# TaskManager Frontend (POC)

Minimal Vite + React + TypeScript scaffold.

Install and run:

```bash
cd frontend
npm install
npm run dev
```

Next steps:
- Implement auth integration (login/register) using the backend API
- Fetch `/api/tasks/occurrences/` and render calendar views
